const items=[['High','Public filing excerpt','Revenue concentration decreased in the sample period.'],['Open','Missing source','No current independent retention data in the demo record.'],['High','Operator transcript','Management described a new implementation process.'],['Counter','Analyst challenge','The reported improvement may be seasonal rather than durable.']];const target=document.querySelector('#evidence');function render(filter='all'){target.innerHTML=items.filter(x=>filter==='all'||(filter==='high'&&x[0]==='High')||(filter==='open'&&x[0]==='Open')).map(([type,title,text])=>`<div class="row"><div><span class="badge ${type==='High'?'acid':type==='Open'?'warn':''}">${type.toUpperCase()}</span><h2 style="margin-top:9px">${title}</h2><p class="activity">${text}</p></div></div>`).join('')}document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)});render();
