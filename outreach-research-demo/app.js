const requirements=['Relevant public source is attached','Observed problem is stated without inference','Counterargument or uncertainty is recorded','Discovery question is drafted'];const list=document.querySelector('#checks');list.innerHTML=requirements.map((x,i)=>`<label class="check"><input type="checkbox" data-i="${i}"><span>${x}</span></label>`).join('');const boxes=[...document.querySelectorAll('input')];const percent=document.querySelector('#percent'),bar=document.querySelector('#bar'),prepare=document.querySelector('#prepare'),status=document.querySelector('#status'),result=document.querySelector('#result');const update=()=>{const n=boxes.filter(x=>x.checked).length,p=Math.round(n/boxes.length*100);percent.textContent=p+'%';bar.style.width=p+'%';prepare.disabled=n!==boxes.length;status.textContent=n===boxes.length?'Ready for a reviewed discovery brief':'Evidence review incomplete';};boxes.forEach(x=>x.onchange=update);prepare.onclick=()=>result.textContent='Synthetic brief prepared locally. No message was created or sent.';update();
