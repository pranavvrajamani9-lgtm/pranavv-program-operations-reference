{
  "name": "program-operations-reference",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/http/server.js",
    "test": "node --test tests/**/*.test.js",
    "verify": "npm test"
  },
  "engines": { "node": ">=20" }
}
